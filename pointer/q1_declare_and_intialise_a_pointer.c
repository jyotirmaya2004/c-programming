//Declare and initialize a pointer
#include <stdio.h>
int main() {
    int a = 10;
    int *p = &a;  // Pointer initialization
    printf("Value of a: %d\n", *p);  // Dereferencing pointer
    return 0;
}
